{
  "name": "reef",
  "private": true,
  "type": "module",
  "description": "REEF \u2014 Netlify functions deps (jose verifies Privy access tokens; viem for on-chain reads; @neondatabase/serverless talks to Postgres over HTTPS). The site itself is static.",
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "jose": "^5.9.6",
    "viem": "^2.21.0"
  }
}
